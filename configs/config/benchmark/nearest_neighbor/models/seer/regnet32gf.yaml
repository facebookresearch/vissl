# @package _global_
config:
  MODEL:
    TRUNK:
      NAME: regnet
      REGNET:
        name: regnet_y_32gf
    HEAD:
      PARAMS: [
        ["swav_head", {"dims": [3712, 4096, 4096, 256], "use_bn": True, "num_clusters": []}],
      ]
