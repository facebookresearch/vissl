# @package _global_
config:
  DATA:
    TRAIN:
      DATA_SOURCES: [everstore]
      DATASET_NAMES: [openimages_1M]
      MMAP_MODE: True
      COPY_TO_LOCAL_DISK: True
      COPY_DESTINATION_DIR: /tmp/openimages_1M/
      DROP_LAST: True
  MACHINE:
    NUM_DATALOADER_WORKERS: 8
